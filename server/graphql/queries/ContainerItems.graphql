query ContainerItems($containerId: ID!) {
  containerItems(containerId: $containerId) {
    id
    userId
    itemId
    containerId
    originalQuantity
    quantityUsed
    expiration
    itemStatus
    price
  }
}
