query ContainerQuery($id: ID!) {
  container(id: $id) {
    id
    name
    users {
      id
      firstName
      lastName
    }
    items {
      id
      name
      imageUrl
      containerItem {
        id
        userId
        originalQuantity
        quantityUsed
        expiration
        itemStatus
      }
    }
  }
}
