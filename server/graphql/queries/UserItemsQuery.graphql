query UserItemsQuery($id: ID!) {
  user(id: $id) {
    containerItems {
      item {
        name
        imageUrl
      }
      createdAt
      id
      originalQuantity
      quantityUsed
      expiration
      price
      itemStatus
      imageUrl
    }
  }
}
