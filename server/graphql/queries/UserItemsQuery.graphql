query UserItemsQuery($id: ID!) {
  user(id: $id) {
    containerItems {
      item {
        name
      }
      createdAt
      id
      originalQuantity
      quantityUsed
      expiration
      price
      itemStatus
    }
  }
}
